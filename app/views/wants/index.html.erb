<div class="container">
  <div class="row mb-4">
    <% @wants.each do |want| %>
      <% if want.item.buying == 'soldout' %>
  
      <% else %>
        <div class="col-6 col-lg-3 favorite">
          <% if want.item.image? %>
            <%= image_tag want.item.image.url(:thumb) %>
          <% end %>
        </div>
        <div class="col-6 col-lg-3 favorite2">
          <div>
            出品者：<%= want.item.user.name %>
          </div>
          <div>
            商品名：<%= link_to want.item.title, item_path(want.item) %>
          </div>
          <div>
            価格　：<%= want.item.price %>円
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
  <%= paginate @wants %>
</div>